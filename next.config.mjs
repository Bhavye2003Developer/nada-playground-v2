/** @type {import('next').NextConfig} */
const nextConfig = {
  // webpack(config, { isServer }) {
  //   if (!isServer) {
  //     // Example: Apply plugin only on client-side builds
  //     config.plugins.push(new PyodidePlugin());
  //   }
  //   return config;
  // },
  // output: "export",
  // basePath: "/sar-in-browser",
  // images: {
  //   unoptimized: true,
  // },
};

export default nextConfig;
